\begin{definition}
For a prefix \(p\) and an integer \(r\ge0\), let
\[
  U_r^p=\{\,y\in V(G(t,K)):\ r^p(y)\le r\,\},
\]
where \(r^p(y)\) is the prefix rank of \(\noderef{StarProductPrefixRank}\).
\end{definition}
