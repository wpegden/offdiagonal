\begin{definition}
For a vertex type \(V\), a loop graph on \(V\) is a binary edge relation
\[
  E \colon V \to V \to \mathrm{Prop}.
\]
The relation may hold when the two arguments are equal, so loops are allowed.
\end{definition}
