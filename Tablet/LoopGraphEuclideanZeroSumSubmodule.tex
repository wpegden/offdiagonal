\begin{definition}
For a finite vertex type \(V\), the Euclidean zero-sum subspace is the real
linear subspace of \(\ell^2(V)\) consisting of all vectors \(x\) whose
coordinates have sum zero:
\[
  W_0(V)=\left\{x\in \ell^2(V): \sum_{v\in V} x(v)=0\right\}.
\]
\end{definition}
