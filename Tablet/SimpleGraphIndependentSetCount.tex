\begin{definition}
For a finite simple graph \(G\), \(i_k(G)\) is the number of independent vertex sets of size \(k\). Equivalently, it is the number of \(k\)-cliques in the complement graph.
\end{definition}
