\begin{definition}
A finite loop graph \(G\) is an \((n,d,\lambda)\)-graph if it has \(n\) vertices, is symmetric, every vertex has degree \(d\), every non-principal adjacency eigenvalue has absolute value at most \(\lambda\), and \(\lambda\ge 0\).
\end{definition}
