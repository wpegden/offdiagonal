\begin{definition}
For loop graphs \(F\) and \(G\) on the same vertex set, the ordered pair \((F,G)\) is \(H_s\)-free if there is no \(2s\)-tuple
\[
  a_1,b_1,\ldots,a_s,b_s
\]
such that \(a_i b_i\in E(F)\) for every \(i\), and \(a_i b_j\in E(G)\) for every \(i<j\).
\end{definition}
