\begin{definition}
The two-color Ramsey number \(r(s,k)\) is the least integer \(n\) such that every graph on \(n\) vertices contains a clique of size \(s\) or an independent set of size \(k\).
\end{definition}
