\begin{lemma}
Let \(s\ge 2\), let \(K\) be a field, and put \(G=G(s-2,K)\). If \(F=\overline G\), where loops are complemented as well, then the pair \((F,G)\) is \(H_s\)-free.
\end{lemma}
\begin{proof}
SKETCH:
An \(H_s\) configuration in \((\overline G,G)\) is precisely a list \(a_1,b_1,\ldots,a_s,b_s\) for which \(a_i b_i\notin E(G)\) for every \(i\), and \(a_i b_j\in E(G)\) whenever \(i<j\). With \(t=s-2\), this is exactly the skew bipartite configuration of size \(t+2=s\) excluded by \noderef{PolarityGraphSkewFree}.
\end{proof}
