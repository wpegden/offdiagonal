\begin{definition}
A loop graph \(G\) has no skew bipartite configuration of size \(s\) if there are no vertices
\[
  a_1,b_1,\ldots,a_s,b_s
\]
such that \(a_i b_j\in E(G)\) for every \(i<j\), while \(a_i b_i\notin E(G)\) for every \(i\).
\end{definition}
