\begin{definition}
For a finite loop graph \(G\), the adjacency action of \(G\) on a real-valued function \(f\colon V\to \mathbb R\) is the function
\[
  (A_G f)(v)=\sum_{w\in V:\,vw\in E(G)} f(w).
\]
Loops are included in the sum exactly once.
\end{definition}
