\begin{definition}
A loop graph \(G\) is symmetric if for all vertices \(x,y\), the implication
\[
  xy \in E(G) \Longrightarrow yx \in E(G)
\]
holds. In this tablet, symmetric loop graphs model the undirected loop graphs used in the paper.
\end{definition}
