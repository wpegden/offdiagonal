\begin{definition}
Let \(G\) be a loop graph.  A \(2m\)-tuple
\[
  (a_1,b_1,\ldots,a_m,b_m)
\]
of vertices of \(G\) is \(D^*\)-consistent if \(a_i b_i\in E(G)\) for every
\(i\), and whenever \(i<j\) and \(a_i b_j\in E(G)\), one also has
\(a_j b_i\in E(G)\).
\end{definition}
