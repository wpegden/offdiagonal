\begin{definition}
For a prefix \(p\) and an integer \(r\ge0\), let
\[
  Z_r^p=\{\,y\in V(G(t,K)):\ r^p(y)=r\,\}.
\]
Equivalently, this is the layer \(U_r^p\setminus U_{r-1}^p\) from the
paper, using the rank \(r^p(y)\) of \(\noderef{StarProductPrefixRank}\).
\end{definition}
