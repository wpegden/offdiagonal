\begin{definition}
Given a loop graph \(F\) on \(V\), the vertex type of the product digraph associated to \(F\) is the set of ordered pairs \((a,b)\in V^2\) such that \(ab\in E(F)\).
\end{definition}
